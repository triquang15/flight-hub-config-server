server:
  port: 8085

spring:
  datasource:
    url: ${LOCATION_DATASOURCE_URL}
    username: ${LOCATION_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME}}
    password: ${LOCATION_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD}}
    driver-class-name: ${LOCATION_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
