server:
  port: 8085

spring:
  datasource:
    url: ${LOCATION_DATASOURCE_URL:jdbc:postgresql://localhost:5432/airline_location_db}
    username: ${LOCATION_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME:postgres}}
    password: ${LOCATION_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD:12345678}}
    driver-class-name: ${LOCATION_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
  data:
    redis:
      host: ${LOCATION_REDIS_HOST:${SPRING_DATA_REDIS_HOST:localhost}}
      port: ${LOCATION_REDIS_PORT:${SPRING_DATA_REDIS_PORT:6379}}
      timeout: ${LOCATION_REDIS_TIMEOUT:2000ms}
      lettuce:
        pool:
          max-active: ${LOCATION_REDIS_POOL_MAX_ACTIVE:8}
          max-idle: ${LOCATION_REDIS_POOL_MAX_IDLE:4}
          min-idle: ${LOCATION_REDIS_POOL_MIN_IDLE:1}
  cache:
    type: redis

app:
  media-service:
    base-url: ${MEDIA_SERVICE_BASE_URL:http://localhost:8089}
