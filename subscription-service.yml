server:
  port: 8089

spring:
  datasource:
    url: ${SUBSCRIPTION_DATASOURCE_URL:jdbc:postgresql://localhost:5432/airline_subscription_db}
    username: ${SUBSCRIPTION_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME:postgres}}
    password: ${SUBSCRIPTION_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD:12345678}}
    driver-class-name: ${SUBSCRIPTION_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
