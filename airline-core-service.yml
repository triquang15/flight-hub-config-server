server:
  port: 8081

spring:
  datasource:
    url: ${AIRLINE_CORE_DATASOURCE_URL}
    username: ${AIRLINE_CORE_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME}}
    password: ${AIRLINE_CORE_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD}}
    driver-class-name: ${AIRLINE_CORE_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
