server:
  port: 8091

spring:
  datasource:
    url: ${NOTIFICATION_DATASOURCE_URL}
    username: ${NOTIFICATION_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME}}
    password: ${NOTIFICATION_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD}}
    driver-class-name: ${NOTIFICATION_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
