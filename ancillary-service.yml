server:
  port: 8082

spring:
  datasource:
    url: ${ANCILLARY_DATASOURCE_URL:jdbc:postgresql://localhost:5432/airline_ancillary_db}
    username: ${ANCILLARY_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME:postgres}}
    password: ${ANCILLARY_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD:12345678}}
    driver-class-name: ${ANCILLARY_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
