server:
  port: 8082

spring:
  datasource:
    url: ${ANCILLARY_DATASOURCE_URL}
    username: ${ANCILLARY_DATASOURCE_USERNAME:${FLIGHTHUB_DATASOURCE_USERNAME}}
    password: ${ANCILLARY_DATASOURCE_PASSWORD:${FLIGHTHUB_DATASOURCE_PASSWORD}}
    driver-class-name: ${ANCILLARY_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
